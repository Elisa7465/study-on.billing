nelmio_api_doc:
    documentation:
        info:
            title: StudyOn Billing API
            description: API для авторизации, регистрации и получения текущего пользователя
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
            security:
                -   Bearer: [ ]
    areas:
        path_patterns:
            - ^/api